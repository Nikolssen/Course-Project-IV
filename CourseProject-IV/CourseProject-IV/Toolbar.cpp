#include "Toolbar.h"
Toolbar::Toolbar() {
	this->window = nullptr;
}

void Toolbar::Configure() {

}

LRESULT CALLBACK Toolbar::WindowProc(HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam) {
	return 0;
}